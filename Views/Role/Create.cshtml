@model EmployeeDirectoryProject.ViewModel.CreateRoleViewModel

@{
    ViewData["Title"] = "Create";
}

<div class="mt-2">
    <h3>Create Role</h3>
</div><hr />
<div class="align-items-start justify-content-center">
<div class="row">
    <div class="col-md-8 bg-white p-2">
        <form method="post">
            <div class="form-group row m-1 bg-white">
                <label asp-for="RoleName" class="col-sm-3 text-dark">RoleName</label>
                <div class="col-sm-9">
                <input asp-for="RoleName" class="form-control form-control-sm m-0" placeholder = " Enter Role Name">
                <span asp-validation-for="RoleName" class="text-danger"></span>
                </div>
            </div>
            <div class="text-end m-2">
                <button type="submit" class="btn btn-primary text-white">Create</button>
                <div class="btn btn-dark text-white"> 
                    @Html.ActionLink("Cancel","Index","Role", new{@class="text-white"})
                </div>
            </div>
        </form>
    </div>      
</div>



@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}


